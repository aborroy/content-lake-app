<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.hyland</groupId>
        <artifactId>content-lake-app</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>rag-service</artifactId>
    <name>RAG Service</name>
    <description>RAG (Retrieval-Augmented Generation) service for Alfresco Content Lake</description>

    <dependencies>
        <dependency>
            <groupId>org.hyland</groupId>
            <artifactId>content-lake-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hyland</groupId>
            <artifactId>content-lake-spi</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
            Entra ID tokens for the query-path group resolver, and nothing else. Deliberately not the Graph
            client the SharePoint connector uses: that one lives inside a shaded plugin jar this service never
            loads, and its resource-unit meter and tenant-wide throttle pause are built for a crawl. This
            makes one cheap call per user per cache window, so what it needs from msal4j is the token cache.
        -->
        <dependency>
            <groupId>com.microsoft.azure</groupId>
            <artifactId>msal4j</artifactId>
            <version>1.26.0</version>
        </dependency>
        <!-- Spring AI RAG module: DocumentRetriever, RetrievalAugmentationAdvisor, Query.
             Version managed by spring-ai-bom (imported in the root pom). The Advisor API
             (BaseAdvisor, CallAdvisor, StreamAdvisor) comes transitively via spring-ai-client-chat. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-rag</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- JWT validation for Entra caller authentication (#168): NimbusJwtDecoder, the JWKS client and
             key rotation. Deliberately this rather than spring-boot-starter-oauth2-resource-server, which
             also brings the servlet autoconfiguration that would register its own bearer filter and its own
             AuthenticationManager beside the one RagSecurityConfig builds. The decoder is the only part
             wanted here; the chain is already ours. Version managed by the Boot BOM. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-jose</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Bucket4j: per-principal token-bucket rate limiting (#75). Not managed by any BOM,
             so pin an explicit version. -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <!-- Spring AI MCP server (WebMVC/synchronous) exposing rag-service tools over the Model
             Context Protocol (#61). Version managed by spring-ai-bom. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>
        <!-- Semantic query-result caching (#72): bounded, short-TTL Caffeine caches for query
             embeddings and full retrieval results. Both versions are managed by the Spring Boot BOM
             (spring-boot-starter-parent), so no explicit version is pinned. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- Distributed tracing (#73): Micrometer Tracing bridged to OpenTelemetry plus the OTLP
             span exporter. Versions managed by the Spring Boot BOM. The OTLP exporter endpoint is
             unset by default (management.otlp.tracing.endpoint), so no spans leave the process and
             no collector is required until one is configured. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
